{/*
    Problem 1
    Given the array const numbers = [1, 2, 2, 3, 4, 4, 5], 
    write a function removeDuplicates that returns a new array with all duplicates removed.
    removeDuplicates(numbers); // [1, 2, 3, 4, 5]
*/}

/*Map*/
const anss = new Map();
numbers.map( number => ans.set( number, 1) );
console.log(anss);

// /* Setify */
// const numbers = [1, 2, 2, 3, 4, 4, 5];
// const ans = [...new Set(numbers)];
// console.log(ans);

